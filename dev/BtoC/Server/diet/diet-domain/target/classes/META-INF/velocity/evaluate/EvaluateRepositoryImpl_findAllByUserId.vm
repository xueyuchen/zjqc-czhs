SELECT
	e.evaluate_id AS evaluateId,
	(
		CASE
		WHEN e.admin_user_id IS NULL THEN
			0
		ELSE
			1
		END
	) AS status,
	e.evel_date AS evelDate
FROM
	t_evaluate e
INNER JOIN t_user u ON u.user_id = e.user_id
AND u.del_flg = 0
AND u.user_id = :userId
WHERE
	e.del_flg = 0
AND e.evel_date <  :eatDt